"""tvastar.contrib — optional, high-level extensions built on the core harness.

Nothing in this package is imported by tvastar core. Each submodule may carry
its own optional dependencies (e.g. sentence-transformers for ltm) and is
completely independent of the others.
"""
